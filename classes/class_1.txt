Class Employee

onShiftDay(string)
timeWorked(float)
salary(boolean)
anniversaryDate (string)
payAmount(float)
award[string]

addShift(adds a day to the onShiftDay string);

addAward(adds an award to the awarded string);

addManager(turns the salary boolean to true);

sumWeeklyPay(takes timeWorked and multiples payAmount)
